{
  "name": "brewvault",
  "version": "0.2.1",
  "description": "Render Markdown notes into Homebrewery-style D&D 5e documents inside Obsidian.",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs watch",
    "build": "node scripts/build-css.mjs production && tsc --noEmit --skipLibCheck && node esbuild.config.mjs production",
    "typecheck": "tsc --noEmit --skipLibCheck",
    "test": "node scripts/run-tests.mjs",
    "version": "node version-bump.mjs && git add manifest.json versions.json"
  },
  "keywords": [
    "obsidian",
    "plugin",
    "homebrewery",
    "dnd"
  ],
  "author": "luiserivaldo",
  "license": "MIT",
  "devDependencies": {
    "@types/markdown-it": "^14.2.0",
    "@types/node": "^20.11.0",
    "builtin-modules": "^3.3.0",
    "esbuild": "^0.20.0",
    "obsidian": "^1.5.7",
    "tslib": "2.6.2",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "markdown-it": "^14.1.0"
  }
}
